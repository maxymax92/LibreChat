services:
  api:
    volumes:
    - type: bind
      # CRITICAL FIX: The source path now correctly points to the 'utils/docker' subdirectory
      source: ./utils/docker/librechat.yaml 
      # The target path inside the container remains the same
      target: /app/librechat.yaml
