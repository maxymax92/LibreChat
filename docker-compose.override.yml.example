services:
  api:
    # This mount tells Docker: "Use the librechat.yaml file I created
    # in the root folder, and put it inside the container at the expected path."
    volumes:
    - type: bind
      source: ./librechat.yaml 
      target: /app/librechat.yaml
